[project]
name = "livestock-service"
version = "0.1.0"
description = "livestock Service for AgriScheme Pro"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
