[project]
name = "iot-service"
version = "0.1.0"
description = "iot Service for AgriScheme Pro"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
