# Production environment configuration
# Unified API Gateway for all services via Traefik

# Server IP
SERVER_IP=213.32.19.116

# Unified API Gateway URL
# All services are accessible via path-based routing through the API gateway
# Format: http://{gateway}/api/v1/{service}
EXPO_PUBLIC_API_URL=http://213.32.19.116/api/v1

# Alternative: Use domain name when DNS is configured
# EXPO_PUBLIC_API_URL=https://api.agrischeme.com/api/v1
